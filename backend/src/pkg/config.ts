export interface DbConfig {
    dbUsername: string;
    dbHost: string;
    dbName: string;
    dbPort: number;
}

export interface AppConfig {
    appName: string;
    appPort: number;
    appAddress: string;
}